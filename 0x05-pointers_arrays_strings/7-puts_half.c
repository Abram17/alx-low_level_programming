#include <stdio.h>
#include "main.h"

/**
 * puts_half -  prints half of a string
 *
 * @str: input string
 */

void puts_half(char *str)
{
	for (i = 0; s[i] != '\0'; ++i)
		;
	for (i /= 2; s[i] != '\0'; i++)
		_putchar(s[i]);
	_putchar('\n');
}
